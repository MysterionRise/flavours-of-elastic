# Core (validate.py, load_data.py)
requests>=2.31.0

# Data pipeline (generate_descriptions.py, generate_embeddings.py, index.py)
aiohttp>=3.9.0
sentence-transformers>=3.0.0
elasticsearch>=8.0.0

# Embedding service (embedding_service.py)
fastapi>=0.110.0
uvicorn>=0.29.0
